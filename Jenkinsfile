pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            sh 'docker version'
         }
      }
      stage('Deploy') {
         steps {
            echo 'Hello Gooner'
         }
      }

   }
}