pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            docker version
         }
      }
      stage('Deploy') {
         steps {
            echo 'Hello Gooner'
         }
      }

   }
}