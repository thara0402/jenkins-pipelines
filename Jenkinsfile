pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            echo 'Hello Jenkins'
         }
      }
      stage('Deploy') {
         steps {
            echo 'Hello Gooner'
         }
      }

   }
}