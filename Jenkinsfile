pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            sh 'sudo docker version'
         }
      }
      stage('Deploy') {
         steps {
            echo 'Hello Gooner'
         }
      }

   }
}